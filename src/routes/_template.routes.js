// TEMPLATE: Route definitions
// Routes map HTTP methods + paths to controller functions.
// Replace placeholders with your actual controller imports and route paths.

import { Router } from 'express';
// import { create, list, getById, updateById, deleteById } from '../controllers/ModelName.controller.js';

const router = Router();

// POST /api/modelname
// router.post('/', create);

// GET /api/modelname
// router.get('/', list);

// GET /api/modelname/:id
// router.get('/:id', getById);

// PUT /api/modelname/:id
// router.put('/:id', updateById);

// DELETE /api/modelname/:id
// router.delete('/:id', deleteById);

export default router;